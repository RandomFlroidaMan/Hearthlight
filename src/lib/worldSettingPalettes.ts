/** Shared between client (setting-creation dropdown) and server
 * (src/server/art/artDirection.ts, which maps each key to its actual
 * palette text) — the keys must match exactly. */
export const PALETTE_KEYS = ["forest", "mountain", "coast", "night"] as const;
